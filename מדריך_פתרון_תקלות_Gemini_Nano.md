# מדריך מקיף לפתרון תקלות וטעינת Gemini Nano 🛠️⚡

מדריך זה מרכז את **כל הסיבות האפשריות** לכך שהמודל המקומי של Gemini Nano אינו נטען בדפדפן, ואת הפתרונות המדויקים שלב-אחר-שלב לכל בעיה.

---

## 📑 תוכן עניינים מהיר:
1. [תקלה 1: דגלי ה-AI בדפדפן כבויים (`AI לא זוהה בדפדפן`)](#1-דגלי-ה-ai-בדפדפן-כבויים)
2. [תקלה 2: קבצי המודל עדיין לא הורדו למחשב (`Status: 0.0.0.0`)](#2-קבצי-המודל-עדיין-לא-הורדו-למחשב)
3. [תקלה 3: חוסר במקום פנוי בכונן C: (דרישת 22GB+)](#3-חוסר-במקום-פנוי-בכונן-c)
4. [תקלה 4: האצת חומרה (GPU) כבויה ב-Chrome](#4-האצת-חומרה-gpu-כבויה-ב-chrome)
5. [תקלה 5: מצב חיסכון בסוללה או חיבור רשת מדוד](#5-מצב-חיסכון-בסוללה-או-חיבור-מדוד)
6. [תקלה 6: פרופיל Chrome מנוהל (חשבון עבודה / ארגון / בית ספר)](#6-פרופיל-chrome-מנוהל)
7. [תקלה 7: שרת ה-Python המקומי לא מגיב או פורט תפוס](#7-שרת-ה-python-המקומי-אינו-מגיב)

---

## 1. דגלי ה-AI בדפדפן כבויים
### 🔍 הסימפטום:
בצ'אט מופיעה שגיאה: `AI לא זוהה בדפדפן` או `Prompt API אינו זמין`.

### 💡 הסיבה:
כרום חוסם את ממשקי ה-AI המקומיים כברירת מחדל עד להפעלת הדגלים המתאימים.

### 🛠️ הפתרון:
1. פתח לשונית חדשה ב-Chrome והדבק את הכתובת:
   `chrome://flags/#prompt-api-for-gemini-nano`
   * שנה את ההגדרה מ-**Default** ל-**Enabled**.
2. לאחר מכן עבור לכתובת:
   `chrome://flags/#optimization-guide-on-device-model`
   * שנה את ההגדרה מ-**Default** ל-**Enabled BypassPerfRequirement** (חשוב מאוד לבחור ב-Bypass כדי לעקוף בדיקות ביצועים מחמירות).
3. לחץ על כפתור **Relaunch** (הפעלה מחדש) שמופיע בתחתית הדפדפן.

---

## 2. קבצי המודל עדיין לא הורדו למחשב
### 🔍 הסימפטום:
הדגלים מופעלים, אך המודל תקוע על `מוריד קבצים למחשב...` או `AI לא זמין`.

### 💡 הסיבה:
המודל של Gemini Nano שוקל כ-**1.5GB עד 2GB**. כרום מוריד אותו ברקע כרכיב מערכת (Component).

### 🛠️ הפתרון:
1. פתח לשונית חדשה והיכנס לכתובת:
   `chrome://components`
2. חפש ברשימה את הרכיב:
   **`Optimization Guide On Device Model`**
3. בדוק מה הסטטוס:
   * אם הגרסה היא `0.0.0.0` או כתוב `Component not downloaded` ⬅️ לחץ על כפתור **Check for update** (בדוק אם יש עדכון).
   * תראה שהסטטוס משתנה ל-`Downloading...` ואחר כך ל-`Up to date`.
   * ברגע שמופיע מספר גרסה אמיתי (למשל `2024.11.x.x`), המודל הורד במלואו ומוכן לעבודה מיידית!

---

## 3. חוסר במקום פנוי בכונן C:
### 🔍 הסימפטום:
הורדת המודל נכשלת שוב ושוב או נעצרת ב-`0.0.0.0`.

### 💡 הסיבה:
Google Chrome דורש לפחות **22GB של מקום פנוי בכונן המערכת (C:)** כדי לאפשר פריקה והרצה של מודלי בינה מלאכותית מקומיים.

### 🛠️ הפתרון:
1. פתח את "המחשב שלי" ובדוק את כונן `C:`.
2. וודא שיש לפחות **25GB פנויים**.
3. נקה קבצים זמניים (באמצעות `ניקוי הדיסק` של Windows או מחיקת קבצים כבדים).
4. חזור ל-`chrome://components` ולחץ שוב על **Check for update**.

---

## 4. האצת חומרה (GPU) כבויה ב-Chrome
### 🔍 הסימפטום:
המודל אינו מצליח לרוץ או פולט שגיאות GPU / WebGPU / DirectML.

### 💡 הסיבה:
Gemini Nano עושה שימוש במאיץ הגרפי (כרטיס מסך מובנה או ייעודי) להרצת טוקנים במהירות.

### 🛠️ הפתרון:
1. היכנס להגדרות כרום:
   `chrome://settings/system`
2. וודא שהאפשרות:
   **"השתמש בהאצת גרפיקה כשהיא זמינה" (Use graphics acceleration when available)**
   מסומנת כ-**פעילה (ON)**.
3. הפעל מחדש את הדפדפן.

---

## 5. מצב חיסכון בסוללה או חיבור מדוד
### 🔍 הסימפטום:
בלפטופים – המודל עובד כשהמחשב מחובר לחשמל, אך מפסיק לעבוד בסוללה.

### 💡 הסיבה:
מנגנון חסכון באנרגיה של Windows / Chrome מקפיא תהליכי רקע כבדים של בינה מלאכותית.

### 🛠️ הפתרון:
1. חבר את המחשב למטען / לחשמל.
2. ב-Chrome היכנס ל: `chrome://settings/performance`
3. כבה את `חיסכון באנרגיה` (Energy Saver) עבור הכתובת המקומית.
4. וודא שהרשת אינה מוגדרת כ"חיבור מדוד" (Metered Connection) בהגדרות ה-Wi-Fi של Windows.

---

## 6. פרופיל Chrome מנוהל
### 🔍 הסימפטום:
הדגלים ב-`chrome://flags` מופיעים באפור או שאי אפשר לשנות אותם.

### 💡 הסיבה:
חשבונות Google ארגוניים (עבודה, אוניברסיטה, בית ספר) חוסמים תכונות ניסיוניות ומודלי AI של כרום.

### 🛠️ הפתרון:
1. פתח פרופיל Chrome פרטי (Personal Profile) שאינו משויך לארגון.
2. או פתח את האפליקציה דרך **Chrome Canary** או פרופיל משתמש חדש.

---

## 7. שרת ה-Python המקומי אינו מגיב
### 🔍 הסימפטום:
בצ'אט מופיע שרת לא זוהה, או שפקודות פייתון / שמירת מפתחות API נכשלות.

### 🛠️ הפתרון:
1. לחץ פעמיים על הקובץ **`Gemini_Nano.pyw`** או על קיצור הדרך **`Gemini Nano`** בשולחן העבודה.
2. אם הפורט `8765` תפוס ע"י תוכנה אחרת:
   * פתח את הקובץ `server_config.json` בעורך טקסט.
   * שנה את `"port": 8765` לפורט אחר, למשל `"port": 8790`.
   * שמור והפעל מחדש את `Gemini_Nano.pyw`.

---

## 🚀 כלי עזר אוטומטי מהיר
בתיקיית הפרויקט נמצא קובץ בשם:
**`enable_chrome_gemini_flags.bat`**
לחיצה כפולה עליו תפתח עבורך אוטומטית את כל הלשוניות הרלוונטיות ב-Chrome בלחיצה אחת!
