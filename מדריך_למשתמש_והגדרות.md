# מדריך למשתמש – Gemini Nano Studio Ultra 🌟

ברוכים הבאים ל-**Gemini Nano Studio Ultra**, סביבת עבודה מקומית, פרטית ועוצמתית המבוססת על מודל ה-AI המובנה בדפדפן (On-Device AI).

---

## 📂 מבנה הקבצים והגדרות בתיקייה

1. **`Gemini_Nano.pyw`**: השרת המקומי והמנוע הראשי (פועל ברקע ללא חלונות שחורים).
2. **`gemini_nano_chat.html`**: ממשק המשתמש המלא והמתקדם (UI).
3. **`server_config.json`**: קובץ הגדרות השרת והרשת (כתובת IP, פורט, מצב פתיחה, טמפרטורה ומהירות קול).
4. **`custom_personas.json`**: מאגר פרופילים, תפקידים והנחיות מערכת (System Prompts).
5. **`gemini_logo.ico` / `gemini_logo.png`**: האייקון הרשמי של האפליקציה (ברזולוציה גבוהה).
6. **`Start_Gemini_Nano.bat`**: קובץ הפעלה מהיר בלחיצה כפולה.
7. **`enable_chrome_gemini_flags.bat`**: כלי עזר לבדיקה והפעלה של דגלי הבינה המלאכותית ב-Chrome.

---

## ⚙️ התאמת הגדרות מתקדמות (`server_config.json`)

ניתן לערוך את הקובץ `server_config.json` בכל עורך טקסט כדי לשנות:
* `"host"`: `127.0.0.1` (רק למחשב זה) או `0.0.0.0` / כתובת IP מקומית (לגישה מכל מכשיר ברשת הביתית/משרדית).
* `"port"`: פורט ההרצה (ברירת מחדל `8765`).
* `"launch_mode"`: `"app"` (חלון אפליקציה ייעודי) או `"tab"` (לשונית דפדפן רגילה).
* `"watchdog_timeout_sec"`: זמן כיבוי אוטומטי כשאין שימוש (300 שניות = 5 דקות).
* `"voice_rate"`: מהירות הקראת הטקסט ב-Gemini Live (ברירת מחדל `1.05`).

---

## 🎨 תכונות מרכזיות במערכת

* **שער מפתחים ו-API מקומי (OpenAI Compatible):** יצירת מפתחות API והתחברות ל-Gemini Nano מכל שפה (Python, cURL, Node.js) דרך `http://127.0.0.1:8765/v1/chat/completions`.
* **Live Canvas ותבניות מוכנות:** אפליקציות אינטראקטיביות (לוח Kanban, דשבורד Chart.js, מחשבון פיננסי, משחק ארקייד) עם עריכה והורדת קבצים.
* **מאגר ידע ומסמכים מקומי (Local RAG):** אינדוקס מסמכים וקבצי PDF/קוד לחיפוש ומענה מבוסס מקורות ב-100% פרטיות.
* **ערכות נושא (Themes) והתאמה אישית:** בחירה בין Dark Studio, OLED Black, Midnight Indigo, Cyberpunk Neon ו-Clean Light.
* **סוכני AI אוטונומיים:** מצב מחקר מעמיק (Deep Research Agent) ובוחן קוד (Code Reviewer Agent).
* **הרצת Python מקומית:** הרצת קוד פייתון ישירות על המחשב שלך וצפייה בתוצאות.
* **ייבוא קבצים וסיכום אתרים:** תמיכה ב-PDF, קבצי קוד, טקסט ושליפת אתרים ישירות מ-URL.
* **אבטחה ופרטיות מוחלטת:** כל המידע והשיחות מעובדים מקומית על המחשב שלך (100% On-Device).
